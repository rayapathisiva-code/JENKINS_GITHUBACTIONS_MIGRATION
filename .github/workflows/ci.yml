name: Application CI

on:
  push:
    branches: [main, master]
  pull_request:
  workflow_dispatch:

jobs:
  ci:
    uses: ./.github/workflows/reusable-ci.yml
    with:
      image-name: springboot-jenkins-actions-poc
      java-version: "17"
