apiVersion: v1
kind: Service
metadata:
  name: {{ include "springboot-poc.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "springboot-poc.fullname" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
